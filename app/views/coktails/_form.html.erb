<%= form_for(@coktail) do |f| %>
  <% if @coktail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coktail.errors.count, "error") %> prohibited this coktail from being saved:</h2>

      <ul>
      <% @coktail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
